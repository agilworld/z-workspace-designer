/**
 * constants — shared application constants.
 *
 * @status scaffold
 */

/** Grid spacing in pixels for the canvas snap grid. */
export const CANVAS_GRID_SIZE = 24;

/** Default canvas width in grid units. */
export const CANVAS_WIDTH_UNITS = 40;

/** Default canvas height in grid units. */
export const CANVAS_HEIGHT_UNITS = 30;

/** Tax rate applied to rental subtotal (e.g. 0.08 = 8%). */
export const TAX_RATE = 0.08;
