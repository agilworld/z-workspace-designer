/**
 * ProductCard — a single draggable product thumbnail in the sidebar.
 *
 * @status scaffold
 */

export function ProductCard() {
  return (
    <div className="rounded-lg border border-border bg-surface p-3 text-sm text-text-muted">
      ProductCard — placeholder
    </div>
  );
}
