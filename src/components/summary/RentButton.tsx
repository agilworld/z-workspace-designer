/**
 * RentButton — primary CTA to submit the rental request.
 *
 * @status scaffold
 */

export function RentButton() {
  return (
    <button
      type="button"
      className="w-full rounded-lg bg-primary px-4 py-2.5 text-sm font-semibold text-white shadow-sm transition-colors hover:bg-primary-dark focus:outline-none focus-visible:ring-2 focus-visible:ring-primary focus-visible:ring-offset-2"
    >
      Rent Now — placeholder
    </button>
  );
}
