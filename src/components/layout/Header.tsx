/**
 * Header — top navigation bar with branding and actions.
 *
 * @status scaffold
 */

export function Header() {
  return (
    <header className="glass sticky top-0 z-50 flex h-16 w-full items-center justify-between px-6">
      <span className="text-lg font-semibold text-text-primary">monis.rent</span>
    </header>
  );
}
