/**
 * WorkspaceCanvas — the central drop zone where users assemble their rental space.
 *
 * @status scaffold
 */

export function WorkspaceCanvas() {
  return (
    <main className="relative flex flex-1 items-center justify-center bg-background">
      <span className="text-sm text-text-muted">WorkspaceCanvas — placeholder</span>
    </main>
  );
}
