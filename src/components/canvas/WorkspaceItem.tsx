/**
 * WorkspaceItem — a placed product instance on the canvas.
 *
 * @status scaffold
 */

export function WorkspaceItem() {
  return (
    <div className="absolute flex items-center justify-center rounded-lg border border-primary/30 bg-primary/5 text-xs text-primary">
      WorkspaceItem — placeholder
    </div>
  );
}
