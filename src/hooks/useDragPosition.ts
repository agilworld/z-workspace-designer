/**
 * useDragPosition — hook that tracks pointer position during drag operations.
 *
 * @status scaffold
 */

export function useDragPosition() {
  // TODO: track pointer events and return { x, y, isDragging }
  return { x: 0, y: 0, isDragging: false };
}
