/**
 * useWorkspace — hook to read from and write to WorkspaceContext.
 *
 * @status scaffold
 */

export function useWorkspace() {
  // TODO: consume WorkspaceContext and return state + actions
  return { items: [] as unknown[] };
}
